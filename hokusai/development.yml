---
version: '2'
services:
  exchange:
    command: bundle exec rails server -b 0.0.0.0
    environment:
    - RAILS_ENV=development
    extends:
      file: build.yml
      service: exchange
    ports:
      - 8080:8080
    volumes:
      - ../:/app
